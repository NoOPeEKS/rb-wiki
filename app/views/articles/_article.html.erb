<div id="<%= dom_id article %>">
  <p>
    <strong>Title:</strong>
    <%= article.title %>
  </p>

  <p>
    <strong>Category:</strong>
    <%= article.category.name %>
  </p>

  <p>
    <strong>Body:</strong>
    <%= raw(article.body) %>
  </p>

</div>
